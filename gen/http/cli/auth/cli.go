// Code generated by goa v3.22.2, DO NOT EDIT.
//
// auth HTTP client CLI support package
//
// Command:
// $ goa gen github.com/linuxfoundation/lfx-v2-auth-service/cmd/server/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	authservicec "github.com/linuxfoundation/lfx-v2-auth-service/gen/http/auth_service/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"auth-service (livez|readyz)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` auth-service livez` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		authServiceFlags = flag.NewFlagSet("auth-service", flag.ContinueOnError)

		authServiceLivezFlags = flag.NewFlagSet("livez", flag.ExitOnError)

		authServiceReadyzFlags = flag.NewFlagSet("readyz", flag.ExitOnError)
	)
	authServiceFlags.Usage = authServiceUsage
	authServiceLivezFlags.Usage = authServiceLivezUsage
	authServiceReadyzFlags.Usage = authServiceReadyzUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "auth-service":
			svcf = authServiceFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "auth-service":
			switch epn {
			case "livez":
				epf = authServiceLivezFlags

			case "readyz":
				epf = authServiceReadyzFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "auth-service":
			c := authservicec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "livez":
				endpoint = c.Livez()
			case "readyz":
				endpoint = c.Readyz()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// authServiceUsage displays the usage of the auth-service command and its
// subcommands.
func authServiceUsage() {
	fmt.Fprintln(os.Stderr, `Auth service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] auth-service COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    livez: Check if the service is alive.`)
	fmt.Fprintln(os.Stderr, `    readyz: Check if the service is ready to accept requests.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s auth-service COMMAND --help\n", os.Args[0])
}
func authServiceLivezUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] auth-service livez", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check if the service is alive.`)

	// Flags list

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `auth-service livez`)
}

func authServiceReadyzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] auth-service readyz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check if the service is ready to accept requests.`)

	// Flags list

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `auth-service readyz`)
}
